services:
  - type: web
    name: notesgenai-frontend
    env: node
    buildCommand: npm install && npm run build
    startCommand: node server.js
    envVars:
      - key: VITE_API_URL
        sync: false
      - key: PORT
        value: 3000
